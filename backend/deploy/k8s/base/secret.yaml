apiVersion: v1
kind: Secret
metadata:
  name: sfir-secrets
  namespace: sfir
  labels:
    app.kubernetes.io/name: sfir-backend
    app.kubernetes.io/part-of: sfir
type: Opaque
stringData:
  SFIR_DB_PASSWORD: ""        # Set via sealed secret or external secrets operator
  SFIR_JWT_SECRET_KEY: ""     # Set via sealed secret or external secrets operator
  SFIR_ENCRYPTION_KEY: ""     # Set via sealed secret or external secrets operator
  SFIR_OPENAI_API_KEY: ""     # Set via sealed secret or external secrets operator
  SFIR_ANTHROPIC_API_KEY: ""  # Set via sealed secret or external secrets operator
