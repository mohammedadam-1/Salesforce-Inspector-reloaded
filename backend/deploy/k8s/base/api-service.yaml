apiVersion: v1
kind: Service
metadata:
  name: sfir-api
  namespace: sfir
  labels:
    app.kubernetes.io/name: sfir-backend
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: sfir
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: api
